<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <ul>
      <% if (locals.users) { %> <% users.forEach(user => { %>
      <li>
        ID: <%= user.firstName %>, Name: <%= user.id %> <%= user.lastName %>
      </li>
      <% }) %> <% } %>
    </ul>
    <a href="/create">Create a user</a>
  </body>
</html>
